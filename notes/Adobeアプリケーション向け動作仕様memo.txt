AE・PSCEP版UATラッパの動作仕様

アプリケーション側ドキュメントにxMapプロジェクトのコピーを埋め込む

UATラッパーは、この「埋め込まれたプロジェクト」を操作対象にする（＝内部バッファと同期を取る）

サーバとのリンクがあれば、サーバ上のプロジェクトと同期を行う

ステータスが編集禁止であれば、操作を行わない。（操作が必要な場合はフロート化することは可能）


ラッパーは、アプリケーション内のエージェントを呼び出して必要な情報を引き渡す。
アプリケーション内の情報が必要な場合は、エージェントに対して問い合わせを行う？

